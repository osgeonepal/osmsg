{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install uv && uv sync --frozen"
  },
  "deploy": {
    "startCommand": "uv run litestar --app osmsg.api.app:app run --host 0.0.0.0 --port $PORT"
  }
}
